{
  "name": "memory-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP gateway exposing Hermes memory (recall/prefetch/write) to the harnesses",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "dev": "node --experimental-strip-types src/server.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "//": "TODO: add @modelcontextprotocol/sdk to expose tools over MCP transport."
}
