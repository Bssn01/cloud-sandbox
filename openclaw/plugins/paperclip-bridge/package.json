{
  "name": "openclaw-plugin-paperclip-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "OpenClaw plugin bridging chat channels to the Paperclip orchestrator",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "peerDependencies": {
    "openclaw": "*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
